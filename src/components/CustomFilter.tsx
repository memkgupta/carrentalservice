import { CustomFilterPropsInterface } from '@/types'
import React from 'react'

const CustomFilter = ({title}:CustomFilterPropsInterface) => {
  return (
    <div>CustomFilter</div>
  )
}

export default CustomFilter